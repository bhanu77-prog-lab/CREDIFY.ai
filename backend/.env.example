SECRET_KEY=replace-with-a-long-random-value
CORS_ORIGINS=["https://your-project.vercel.app"]
# PostgreSQL is recommended for hosted deployments. Render provides this value.
# DATABASE_URL=postgresql+psycopg://user:password@host/database
# Optional persistent SQLite path for a paid Render disk or another host.
# DATABASE_URL=sqlite:////opt/render/project/src/data/scamshield.db